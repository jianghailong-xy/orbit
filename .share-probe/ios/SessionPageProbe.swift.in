import OrbitKit
import SwiftUI

// GENERATED by gen.py — TEMPORARY evidence probe (see ../../README.md). The nav bar's Share button and
// the `.sheet(isPresented: $showShare)` block are cut verbatim out of
// src/macos/OrbitApp/Sources/OrbitApp/Views/Console/ConsoleView.swift. The transcript is a stand-in.
struct SessionPageProbe: View {
    @State private var appModel = ProbeAppModel()
    private let sessionID = "new"
    @State private var showShare = false

    var body: some View {
        NavigationStack {
            List {
                Text("Can reviewers read this session without an account?")
                Text("Yes — Share makes a public, read-only link to this transcript.")
                    .foregroundStyle(.secondary)
            }
            .navigationTitle("Share links · T8")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
{TOOLBAR_ITEM}
            }
        }
{SHEET}
    }
}
