import SwiftUI
import OrbitKit

// GENERATED by extract.py — TEMPORARY evidence probe. The two modifier blocks below are cut verbatim
// out of src/macos/OrbitApp/Sources/OrbitApp/Views/Console/ConsoleView.swift: the window-toolbar
// item of its macOS branch, and the sheet both platforms present. Only the transcript is a stand-in.
struct SessionPageStandIn: View {
    let sessionID: String
    let appModel: ProbeAppModel
    @State private var showShare = false

    var body: some View {
        TranscriptStandIn()
{TOOLBAR}
{SHEET}
        // The probe's only addition: press the toolbar button's flag once the window is up.
        .task {
            try? await Task.sleep(nanoseconds: 1_500_000_000)
            showShare = true
        }
    }
}
